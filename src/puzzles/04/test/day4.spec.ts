import PuzzleSolution from "../day4";

describe("Day 4 - Camp Cleanup", () => {
    describe("first puzzle", () => {
        describe("find elves who's cleanup assignment overlaps with another elf's", () => {
            it("should be defined", () => {
                expect(PuzzleSolution).toBeDefined();
            });
        });
    });
});